# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'marleyspoon-code-challenge-iOS' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for marleyspoon-code-challenge-iOS
    pod 'IQKeyboardManagerSwift' 
    pod 'Alamofire' 
    pod 'SVProgressHUD' 
    pod 'SDWebImage' 

  target 'marleyspoon-code-challenge-iOSTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'marleyspoon-code-challenge-iOSUITests' do
    # Pods for testing
  end

end
